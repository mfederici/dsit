train:
  _target_: src.data.CelebA.CelebAWrapper
  root: ${device.data_root}
  split: train
  download: ${device.download_files}

valid:
  _target_: src.data.CelebA.CelebAWrapper
  root: ${device.data_root}
  split: valid
  download: ${device.download_files}

test:
  _target_: src.data.CelebA.CelebAWrapper
  root: ${device.data_root}
  split: test
  download: ${device.download_files}