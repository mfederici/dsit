# @package _global_
defaults:
  - /data: MNIST
  - /architectures: MNIST
  - /optimization: batch_ADAM
  - /model: VIB
  - /evaluation:
      - classification
      - elbo

params:
  beta: 0.01

# Seed for the model
seed: 42

